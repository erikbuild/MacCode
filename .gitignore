build/
node_modules/
*.dsk
*.bin
*.ROM
*.app
.DS_Store

# External dependencies / build environment — gitignore everything except
# each subdir's README. See deps/<name>/README.md for what lives there.
/deps/*/*
!/deps/*/README.md

# Native test binaries (built by tests-native/Makefile)
tests-native/test_wire
tests-native/test_transcript

# Superpowers brainstorming scratch (visual companion)
.superpowers/
